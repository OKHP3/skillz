# Superseded

The pluggable-sink model in this file was replaced by explicit mode branching.
See `mode-detection-and-promotion.md`, `github-mode.md`, and `file-mode.md`.
This stub is retained only because the packaged copy could not be deleted in
place; it ships in no released bundle and is safe to remove.
