interface:
  display_name: "Brand Style Registry"
  short_description: "Capture and apply evidence-backed visual styles"
  default_prompt: "Use $okhp3-askjamie-style-registry to capture or apply the AskJamie visual style profile."
policy:
  allow_implicit_invocation: true
