# Project-local support snapshots - 2026-09-19

These six original SKILL.md contracts are historical recovery snapshots, not
installable skills or current evidence. The active project-local packages were
verified equivalent to their shared distribution counterparts before receiving
the same optional brand profiles, loading instructions, versions, and evidence
limits. No local-specific behavior was removed.

The [support preservation manifest](../support-manifest.json) records
6 files and 85,839 raw bytes from baseline
`ff61152eeda6d7985805c9049840964ca781394b`, with original paths, Git modes, blob identities, and byte hashes.
All unchanged local resources, evaluations, and historical results remain in the
active support packages. The copies here preserve only the six replaced
contracts. Recover to a separate location after checking current contents; never
overwrite the active package blindly.
